#include "main.h"
/**
 * mul - multiplies two integers
 * @a: the first integer
 * @b: the second integer
 *
 * Return: the result of the multiplication
 */
int mul(int a, int b);
{
	int c = a * b;

	printf("%d\n", mul(a, b));
	return (a * b);
}
