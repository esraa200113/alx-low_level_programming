#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: This is a C program prints a message using the puts
 * Return: Always 0 (Success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
